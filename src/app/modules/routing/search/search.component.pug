h5 test
h6 {{data}}
mat-nav-list(role="list")
    mat-list-item(*ngFor="let item of results | async",[routerLink]="['/stop', item.shortName]",role="listitem")
        mat-icon(mat-list-icon) place
        h4(mat-line) {{item?.name}}